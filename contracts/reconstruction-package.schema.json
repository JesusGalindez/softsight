{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/JesusGalindez/softsight/contracts/reconstruction-package.schema.json",
  "title": "reconstruction-package",
  "type": "object",
  "properties": {
    "documentType": {
      "type": "string",
      "enum": [
        "videomesh.reconstruction-package"
      ],
      "description": "Qué documento es. Nunca un nombre suelto y ambiguo como `reconstruction` (D14)."
    },
    "contractVersion": {
      "type": "string",
      "description": "Versión del contrato de frontera; 0.x mientras el contrato esté en DRAFT."
    },
    "contractSchemaSha256": {
      "type": "string",
      "description": "Hash del esquema con el que se escribió el paquete. Opcional mientras el contrato esté en DRAFT; si viene y no está en el registro, el paquete no se interpreta (D16)."
    },
    "packageId": {
      "type": "string",
      "description": "Identidad canónica del paquete. El nombre del directorio es comodidad humana y no se usa para inferirla (D7)."
    },
    "state": {
      "type": "string",
      "enum": [
        "WRITING",
        "SEALED"
      ],
      "description": "Un paquete solo se consume SEALED; cualquier otro estado es PACKAGE_NOT_SEALED (D29)."
    },
    "producer": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Nombre del productor."
        },
        "version": {
          "type": "string",
          "description": "Versión del productor."
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "version"
      ],
      "description": "Quién escribió el paquete, para que el informe pueda decirlo."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "anyOf": [
          {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Identidad del artifact dentro del paquete."
              },
              "path": {
                "type": "string",
                "description": "Ruta relativa a la raíz del paquete. Absoluta o con `..` se rechaza (D6)."
              },
              "bytes": {
                "type": "number",
                "description": "Tamaño en bytes, comprobado antes de analizar."
              },
              "sha256": {
                "type": "string",
                "description": "Hash del contenido en hexadecimal minúscula, 64 caracteres."
              },
              "type": {
                "type": "string",
                "enum": [
                  "TRIANGLE_MESH"
                ],
                "description": "Malla de triángulos."
              },
              "purelyReconstructed": {
                "type": "boolean",
                "description": "Cada región de superficie deriva solo de evidencia de reconstrucción. Recalcular normales, soldar, simplificar o convertir formato la mantienen; rellenar agujeros, completar por IA o modelar a mano la fuerzan a false. No significa «la malla no se ha tocado nunca»."
              }
            },
            "additionalProperties": false,
            "required": [
              "id",
              "path",
              "bytes",
              "sha256",
              "type",
              "purelyReconstructed"
            ]
          },
          {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Identidad del artifact dentro del paquete."
              },
              "path": {
                "type": "string",
                "description": "Ruta relativa a la raíz del paquete. Absoluta o con `..` se rechaza (D6)."
              },
              "bytes": {
                "type": "number",
                "description": "Tamaño en bytes, comprobado antes de analizar."
              },
              "sha256": {
                "type": "string",
                "description": "Hash del contenido en hexadecimal minúscula, 64 caracteres."
              },
              "type": {
                "type": "string",
                "enum": [
                  "POINT_CLOUD"
                ],
                "description": "Nube de puntos, dispersa o densa."
              }
            },
            "additionalProperties": false,
            "required": [
              "id",
              "path",
              "bytes",
              "sha256",
              "type"
            ]
          },
          {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Identidad del artifact dentro del paquete."
              },
              "path": {
                "type": "string",
                "description": "Ruta relativa a la raíz del paquete. Absoluta o con `..` se rechaza (D6)."
              },
              "bytes": {
                "type": "number",
                "description": "Tamaño en bytes, comprobado antes de analizar."
              },
              "sha256": {
                "type": "string",
                "description": "Hash del contenido en hexadecimal minúscula, 64 caracteres."
              },
              "type": {
                "type": "string",
                "enum": [
                  "IMAGE"
                ],
                "description": "Imagen de entrada, con su orientación ya horneada en los píxeles (D33)."
              }
            },
            "additionalProperties": false,
            "required": [
              "id",
              "path",
              "bytes",
              "sha256",
              "type"
            ]
          },
          {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Identidad del artifact dentro del paquete."
              },
              "path": {
                "type": "string",
                "description": "Ruta relativa a la raíz del paquete. Absoluta o con `..` se rechaza (D6)."
              },
              "bytes": {
                "type": "number",
                "description": "Tamaño en bytes, comprobado antes de analizar."
              },
              "sha256": {
                "type": "string",
                "description": "Hash del contenido en hexadecimal minúscula, 64 caracteres."
              },
              "type": {
                "type": "string",
                "enum": [
                  "DEPTH_MAP"
                ],
                "description": "Mapa de profundidad."
              },
              "cameraId": {
                "type": "string",
                "description": "Cámara desde la que se midió. Sin ella un mapa de profundidad no se puede interpretar: el número de cada píxel solo significa algo con unos intrínsecos y una pose detrás, y es el mismo argumento que ata la imagen a su calibración en D10."
              },
              "depthKind": {
                "type": "string",
                "enum": [
                  "OPTICAL_AXIS",
                  "RAY_LENGTH"
                ],
                "description": "Sin valor por defecto y sin inferirlo del proveedor: confundirlos mete un error que crece con el ángulo respecto al centro —cero en el centro, máximo en las esquinas—."
              }
            },
            "additionalProperties": false,
            "required": [
              "id",
              "path",
              "bytes",
              "sha256",
              "type",
              "cameraId",
              "depthKind"
            ]
          }
        ]
      },
      "description": "Ficheros del paquete, cada uno con su forma según el tipo."
    },
    "cameras": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Identidad de la cámara dentro del CameraSet."
          },
          "imageArtifactId": {
            "type": "string",
            "description": "Artifact IMAGE al que pertenecen estos intrínsecos; ata píxeles y calibración (D10)."
          },
          "imageArtifactHash": {
            "type": "string",
            "description": "sha256 de esa imagen, y no solo su identidad: un id se puede reapuntar a otro fichero sin que nada chille, y entonces los intrínsecos describen píxeles que no son los suyos. El hash no (D10)."
          },
          "imageSpace": {
            "type": "string",
            "enum": [
              "ORIGINAL",
              "RECTIFIED"
            ],
            "description": "Qué imagen describen estos intrínsecos. Es lo que impide combinar una calibración rectificada con la imagen distorsionada, o una máscara del frame original con profundidad del rectificado: el error no se ve, porque las dos imágenes tienen el mismo tamaño y el mismo aspecto (D10)."
          },
          "sourceOrientation": {
            "type": "number",
            "description": "Orientación EXIF del original, en grados, **solo como provenance** (D33). `width` y `height` describen la rejilla real, con la orientación ya horneada en los píxeles; nada aguas abajo interpreta píxeles a partir de este campo, y una puerta lo comprueba."
          },
          "width": {
            "type": "number",
            "description": "Ancho de la rejilla real, en píxeles."
          },
          "height": {
            "type": "number",
            "description": "Alto de la rejilla real, en píxeles."
          },
          "pixelOrigin": {
            "type": "string",
            "enum": [
              "TOP_LEFT",
              "BOTTOM_LEFT"
            ],
            "description": "Dónde está el píxel (0,0)."
          },
          "pixelCenter": {
            "type": "string",
            "enum": [
              "CENTER",
              "CORNER"
            ],
            "description": "Si el centro del píxel (0,0) cae en 0,5 o en 0."
          },
          "model": {
            "type": "string",
            "enum": [
              "PINHOLE",
              "OPENCV"
            ],
            "description": "Modelo de cámara. Uno desconocido es CAMERA_MODEL_UNSUPPORTED, no un aviso."
          },
          "cameraAxes": {
            "type": "string",
            "enum": [
              "X_RIGHT_Y_DOWN_Z_FORWARD",
              "X_RIGHT_Y_UP_Z_BACKWARD"
            ],
            "description": "Hacia dónde mira la cámara y hacia dónde crece la altura. El primero es el de visión por computador —COLMAP, OpenCV—; el segundo el de gráficos —OpenGL—. Confundirlos no da una imagen torcida: da una especular en Y con la profundidad invertida, que en un objeto simétrico es igual de plausible. Sin valor por defecto, por eso mismo."
          },
          "worldFromCamera": {
            "type": "array",
            "items": {
              "type": "number"
            },
            "minItems": 16,
            "maxItems": 16,
            "description": "Pose de la cámara en el mundo: 4×4 homogénea, por filas, traslación en 3, 7 y 11, vectores columna (D32). Solo worldFromCamera; la inversa se calcula, no se declara."
          },
          "intrinsics": {
            "type": "object",
            "properties": {
              "fx": {
                "type": "number",
                "description": "Focal en x."
              },
              "fy": {
                "type": "number",
                "description": "Focal en y."
              },
              "cx": {
                "type": "number",
                "description": "Punto principal en x."
              },
              "cy": {
                "type": "number",
                "description": "Punto principal en y."
              }
            },
            "additionalProperties": false,
            "required": [
              "fx",
              "fy",
              "cx",
              "cy"
            ],
            "description": "Distancia focal y punto principal, en píxeles."
          },
          "distortion": {
            "type": "object",
            "properties": {
              "k1": {
                "type": "number",
                "description": "Radial de primer orden."
              },
              "k2": {
                "type": "number",
                "description": "Radial de segundo orden."
              },
              "k3": {
                "type": "number",
                "description": "Radial de tercer orden."
              },
              "p1": {
                "type": "number",
                "description": "Tangencial en x."
              },
              "p2": {
                "type": "number",
                "description": "Tangencial en y."
              }
            },
            "additionalProperties": false,
            "description": "Coeficientes con nombre; ausente es sin distorsión, no ceros implícitos."
          }
        },
        "additionalProperties": false,
        "required": [
          "id",
          "imageArtifactId",
          "imageArtifactHash",
          "imageSpace",
          "width",
          "height",
          "pixelOrigin",
          "pixelCenter",
          "model",
          "cameraAxes",
          "worldFromCamera",
          "intrinsics"
        ]
      },
      "description": "CameraSet canónico. Ausente es un paquete sin cámaras registradas, no un error."
    },
    "scale": {
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "enum": [
            "UNKNOWN",
            "RELATIVE",
            "ABSOLUTE"
          ],
          "description": "Con status != ABSOLUTE, un presupuesto en unidades absolutas se rechaza."
        },
        "source": {
          "type": "string",
          "enum": [
            "NONE",
            "KNOWN_DISTANCE",
            "MARKER",
            "CAMERA_PRIOR",
            "EXTERNAL_MEASUREMENT",
            "MANUAL"
          ],
          "description": "De dónde sale la escala."
        },
        "uncertainty": {
          "type": "object",
          "properties": {
            "model": {
              "type": "string",
              "enum": [
                "NONE",
                "GAUSSIAN",
                "INTERVAL"
              ],
              "description": "Qué modelo describe el valor."
            },
            "value": {
              "type": "number",
              "description": "Magnitud en la unidad de la escala."
            }
          },
          "additionalProperties": false,
          "required": [
            "model",
            "value"
          ],
          "description": "Incertidumbre declarada; sin ella no se reporta precisión que nadie justifica."
        }
      },
      "additionalProperties": false,
      "required": [
        "status",
        "source"
      ],
      "description": "Modelo de escala."
    },
    "frameGraph": {
      "type": "object",
      "properties": {
        "transforms": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "from": {
                "type": "string",
                "enum": [
                  "CAMERA",
                  "RECONSTRUCTION",
                  "ASSET_CANONICAL",
                  "PRODUCTION"
                ],
                "description": "Marco de origen."
              },
              "to": {
                "type": "string",
                "enum": [
                  "CAMERA",
                  "RECONSTRUCTION",
                  "ASSET_CANONICAL",
                  "PRODUCTION"
                ],
                "description": "Marco de destino."
              },
              "matrix": {
                "type": "array",
                "items": {
                  "type": "number"
                },
                "description": "Dieciséis números, fila a fila, aplicados como columna a la derecha."
              },
              "reason": {
                "type": "string",
                "description": "Por qué existe esta transformación."
              },
              "producer": {
                "type": "string",
                "description": "Quién la produjo."
              }
            },
            "additionalProperties": false,
            "required": [
              "from",
              "to",
              "matrix",
              "reason",
              "producer"
            ]
          },
          "description": "Cada arista del grafo, con su matriz y su motivo."
        }
      },
      "additionalProperties": false,
      "required": [
        "transforms"
      ],
      "description": "Transformaciones entre marcos, todas registradas."
    },
    "requiredEvidence": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Identidades de artifact que el contrato exige para certificar. Faltar una da INCONCLUSIVE; faltar evidencia que nadie usa es irrelevante (D8)."
    },
    "budgets": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Qué se presupuesta; identifica, así que es único."
          },
          "units": {
            "type": "string",
            "enum": [
              "ABSOLUTE",
              "RELATIVE_TO_DIAGONAL"
            ],
            "description": "En qué se expresa el máximo. Absoluto exige `scale.status` ABSOLUTE; relativo va sobre la diagonal de la caja envolvente, que es el fallback de D9."
          },
          "unit": {
            "type": "string",
            "description": "La unidad cuando es absoluto, como `m` o `mm`. Prohibida con `RELATIVE_TO_DIAGONAL`: una fracción de diagonal no tiene unidad, y ponerle una es declarar una escala por la puerta de atrás."
          },
          "max": {
            "type": "number",
            "description": "Máximo admitido, en las unidades declaradas."
          }
        },
        "additionalProperties": false,
        "required": [
          "name",
          "units",
          "max"
        ]
      },
      "description": "Presupuestos que el paquete declara. R0 solo comprueba que sean coherentes con la escala (D9); evaluarlos contra lo medido es R9."
    },
    "requires": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Capabilities sin las cuales el paquete no se puede consumir. Una que este binario no sepa hacer deja el paquete UNSUPPORTED: medir sin ella produciría un informe sobre otra cosa (D31)."
    },
    "provides": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Capabilities que el paquete trae. Una desconocida no impide consumirlo, pero se declara en el informe: no decirlo deja al productor sin saber si se usó o se tiró (D31)."
    },
    "extensions": {
      "type": "object",
      "patternProperties": {
        "^[a-z][a-z0-9]*(\\.[a-z0-9-]+){2,}$": {
          "type": "object",
          "properties": {
            "required": {
              "type": "boolean",
              "description": "Si el consumidor no puede interpretar el paquete sin entenderla. Obligatorio y sin valor por defecto: suponerlo `false` convertiría en silencio una extensión que cambia el sentido de los datos."
            },
            "data": {
              "type": "object",
              "description": "Carga de la extensión. Opaca a propósito: es del productor, y darle forma aquí sería declarar como frontera algo que todavía es experimento."
            }
          },
          "additionalProperties": false,
          "required": [
            "required"
          ]
        }
      },
      "additionalProperties": false,
      "description": "Espacio explícito para lo experimental (D30). Fuera de aquí, un campo desconocido es error; dentro, la clave la elige el productor y este binario no tiene por qué conocerla. Una extensión `required` que no se entienda para el consumo con UNSUPPORTED; una opcional se preserva y se declara, nunca se ignora en silencio."
    }
  },
  "additionalProperties": false,
  "required": [
    "documentType",
    "contractVersion",
    "packageId",
    "state",
    "producer",
    "artifacts",
    "scale",
    "frameGraph"
  ]
}
